package com.springboot.data_jpa.dto;

import com.springboot.data_jpa.entity.Passport;
import lombok.Data;

//@Data
public class PassportDtoValidation {
    /*@NotBlank(message = "field series does not hold required symbols")
    private String series;

    @NotBlank(message = "field number does not hold required symbols")
    private String number;

    public Passport toPassport(){
        Passport passport = new Passport();
        passport.setSeries(series);
        passport.setNumber(number);
        return passport;
    }*/
}
